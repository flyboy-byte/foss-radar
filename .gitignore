node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables — never commit these
.env
.env.local
.env.*.local

# Local SQLite database — contains personal data, not for git
fossradar.db

# Migration artifacts
export.json